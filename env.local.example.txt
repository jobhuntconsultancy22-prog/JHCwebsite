# RENAME THIS FILE to exactly:  .env.local
# (it's named without the leading dot here so it doesn't get hidden by your
# OS/file browser when you unzip the project)
#
# Then fill in the real values from your Supabase project
# (Project Settings -> API in the Supabase dashboard).

NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# SERVER-ONLY. Never put this in NEXT_PUBLIC_ and never expose it to the browser.
# Used only in app/api/admin/create-team-member to create team logins.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Used so invite emails link back to the right site
NEXT_PUBLIC_SITE_URL=https://jobhuntconsultancy.in

# Web3Forms access key (safe to expose publicly — see forms.js in the old static
# site for background). Get one at https://web3forms.com
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=your-web3forms-access-key

# Resend — sends the "your application status changed" email to candidates.
# SERVER-ONLY, do not prefix with NEXT_PUBLIC_. Get a free key at https://resend.com
RESEND_API_KEY=your-resend-api-key
# Optional: once you verify jobhuntconsultancy.in as a sending domain in Resend,
# switch this to something like: Job Hunt Consultancy <notifications@jobhuntconsultancy.in>
# Until then, leave it unset and emails will send from Resend's shared test address.
RESEND_FROM_EMAIL=
